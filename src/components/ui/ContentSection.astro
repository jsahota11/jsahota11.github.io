---
interface Props {
  id?: string;
  title?: string;
  class?: string;
}

const { id, title, class: className } = Astro.props;
---

<section
  id={id}
  class:list={[
    className,
  ]}
>
  {
    title && (
      <h2>
        {title}
      </h2>
    )
  }
  <div>
    <slot />
  </div>
</section>
