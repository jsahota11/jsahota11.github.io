---
const { label, link } = Astro.props;

// see if current URL matches button
const isActive = Astro.url.pathname === link;
---

<div class="border-solid rounded-md border-2 border-flame m-1">
    <a href={link} class="nav-link">
        <!-- hovering and clicking will change the state of the button -->
        <button
            class="p-1 w-32 h-10 bg-accent hover:bg-accentHover hover:text-textSecondary active:bg-flame active:text-accentHover
            transition-colors duration-200"
        >
            {label}
        </button>
    </a>
</div>
